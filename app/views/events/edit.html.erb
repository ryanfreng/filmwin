<% provide(:title, "Edit event") %>
<h1>Update your event</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@event) do |e| %>
      <%= render 'fields', e: e %>

      <%= e.submit "Save changes", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>